2026-05-18: Added external Unikraft platform wiring in this repo (`Config.uk`, `Makefile.uk`, `Linker.uk`, `ukplat/*`) plus a tiny default app and `configs/rpi5_defconfig`. Updated top-level `make` to invoke `gmake -C ../unikraft A=$PWD/app P=$PWD O=$PWD/build/unikraft C=$PWD/app/.config UK_CFLAGS=-std=gnu11`. `make` now completes and emits `build/unikraft/kernel_2712.img`; remaining warnings are Unikraft Kconfig warnings about unrelated choice/imply symbols and native TLB selection without paging.
2026-05-18: Verified generated files with `ls -lh build/unikraft/app_rpi5-arm64 build/unikraft/app_rpi5-arm64.dbg build/unikraft/kernel_2712.img`; output image is about 243 KiB and debug ELF is about 1.2 MiB.
2026-05-18: Fixed fresh-tree `make` failure where Unikraft normalized missing `build/` to `/unikraft`; added `unikraft-build-dir` target that runs `mkdir -p $(UK_BUILD_DIR)` before invoking `gmake`. Verified by removing `build/unikraft app/.config` and running `make`, which rebuilt `build/unikraft/kernel_2712.img`.
2026-05-18: Removed assumed BSD-3-Clause SPDX headers from newly added `ukplat/*` and `app/main.c`. Verified no SPDX headers remain in `Config.uk Makefile.uk Linker.uk ukplat app configs src` with `rg`, then reran `make` successfully.
2026-05-19: Added Unikraft platform early spin loop in `ukplat/entry64.S` using `x2` as the continue flag, ported the bare-metal PL011 UART into `ukplat/pl011.[ch]`, added UART boot breadcrumbs around relocation/pcpu/FDT/setup, and updated `debug_cfg/ConnectJTAG.gdb` with `rpi5_reload` to reload the Unikraft debug ELF and restore `x0` from `debug_cfg/bcm2712-rpi-5-b.dtb` at `0x10000000`. Verified with `make`; it rebuilt `build/unikraft/kernel_2712.img`.
2026-05-19: Reran `make` after changing `debug_cfg/ConnectJTAG.gdb` to use `build/unikraft/app_rpi5-arm64.dbg`; build completed with no new compile work, only the existing Kconfig warnings.
2026-05-19: Added `CONFIG_RPI5_EARLY_GDB_SPINLOOP`, default `y`, to guard the early `x2` GDB spin loop in `ukplat/entry64.S`. Verified with `make` and confirmed `CONFIG_RPI5_EARLY_GDB_SPINLOOP=y` in `app/.config` and `build/unikraft/config`.
2026-05-19: Inspected configs/rpi5_defconfig and its Makefile/Kconfig references to explain the default RPi 5 Unikraft build configuration.
2026-05-19: Updated `debug_cfg/ConnectJTAG.gdb` and its `rpi5_reload` helper to set `$x2 = 0` after restoring `$pc` and `$x0`, so the early GDB spin loop is released by the script. No rebuild needed.
2026-05-19: Moved `file build/unikraft/app_rpi5-arm64.dbg` before `target extended-remote :3333` in `debug_cfg/ConnectJTAG.gdb` to avoid GDB warning that no executable was specified before connecting. No rebuild needed.
2026-05-19: Added `rpi5_pl011_dump_mem()` and call it from `ukplat/entry64.S` after printing the DTB pointer to dump the first 64 bytes at `x0`. Verified with `make`; it rebuilt `build/unikraft/kernel_2712.img` with only the existing Kconfig warnings.
2026-05-19: Added EL2-to-EL1 drop, `VBAR_EL1=vector_table`, KVM ARM exception vectors, GICv2 config, PL011 console registration, BSP lcpu init, and removal of the low `vbase=0` free memory region before boot. Verified via `tmux` GDB/tio run: after clearing `rpi5_gdb_spin_flag`, UART reaches `uk_boot_entry()`, and interrupting shows `uk_boot_entry -> uk_pm_shutdown -> uk_pm_syshalt -> lcpu_halt(error_code=0)`.
2026-05-19: Changed the early GDB spin loop to re-read `rpi5_gdb_spin_flag` each pass and updated `debug_cfg/ConnectJTAG.gdb` to release it with `set {unsigned long long}&rpi5_gdb_spin_flag = 0`. Verified with `make`; in `tmux` GDB, manually setting that memory from `1` to `0` released the loop and hit `rpi5_pl011_init`.
2026-05-19: Clarified the `CONFIG_RPI5_EARLY_GDB_SPINLOOP` help text to mention clearing either `x2` at the loop `nop` or `rpi5_gdb_spin_flag`; no rebuild needed.
2026-05-19: `main()` was reached under GDB but `printf()` did not appear on UART because `ukplat/pl011.c` used `CONFIG_LIBUKCONSOLE` without including `uk/config.h`, compiling the console registration path out. Added the include and rebuilt with `make`. Live rerun was blocked after OpenOCD entered a stale FP/SIMD register state (`Failed to write v0 register`) and restarting OpenOCD required sudo password input in tmux pane `SemesterProject:0.0`.
2026-05-28: Restored overwritten `ukplat/entry64.S` from the prior RPi5 bring-up version: EL2-to-EL1 drop, `VBAR_EL1=vector_table`, optional `rpi5_gdb_spin_flag` loop, boot stack/BSS setup, PL011 breadcrumbs, DTB dump, pcpu/FDT setup, and branch to `rpi5_ukplat_entry`. Verified with `make`; it rebuilt `build/unikraft/kernel_2712.img` with only existing Kconfig warnings.
2026-05-28: Added BSD-3-Clause headers to Unikraft-adapted `ukplat/entry64.S`, `ukplat/link64.lds.S`, and `ukplat/memory.c`, with original `ukplat/entry64.S`/`ukplat/link64.lds.S` attribution to Wei Chen and 2026 local-modification copyright for Lorin Urbantat. Verified with `make`, which rebuilt `build/unikraft/kernel_2712.img`.
2026-05-28: Added Lorin Urbantat to the `Authors:` blocks in `ukplat/entry64.S` and `ukplat/link64.lds.S`. Verified with `make`, which rebuilt `build/unikraft/kernel_2712.img`.
2026-05-28: Removed the legacy baremetal boot path (`src/*` and `linker.ld`) and stripped the top-level Makefile baremetal/disasm/flash-baremetal build rules. Verified with `make`, which completed the remaining Unikraft build with only the existing Kconfig warnings.
2026-07-15: Removed the no-op early `do_uk_reloc` path from the fixed-address RPi5 entry, added a pre-VBAR runtime check that parks with the actual base in `x1` and required base in `x2` when the image is not loaded at its linked address, and made `Makefile.uk` reject `CONFIG_OPTIMIZE_PIE=y` for `PLAT_RPI5`.
2026-07-15: Fixed the app build by including the private `app/bruol.h` header with a quoted include. Verified with `make`.
2026-07-15: Added DTB-driven EL1 MMU bootstrap in `ukplat/pagetable64.S`: validated the FDT, decoded enabled root memory `reg` tuples, built 1 GiB Normal identity blocks plus the fixed 64–128 GiB Device window, exported rounded ranges, and enabled TTBR0/MAIR/TCR/SCTLR. Entry now gates Pi 5 cores by MPIDR Aff1 and calls MMU setup before BSS clearing; memory unmap metadata uses the single parsed range. `make -j4` emits `kernel_2712.img`. A tmux/GDB hardware run found and fixed pre-MMU unaligned string loads (ESR `0x96000021` at DT strings `0x100116c4`) by switching comparisons to byte loads; final cold-entry hardware verification remains pending because OpenOCD cannot reset this target and the debug core is currently inside the prior exception context.
2026-07-15: Removed the C-side DTB-range-to-`bpt_unmap_mrd` translation and the deletion of the entire low free region. The platform now supplies only the fixed pre-device bootstrap aperture, reserves page zero through the common memregion insert/coalesce logic, and calls `ukplat_mem_init()` before boot handoff so common memory trimming/allocation stays authoritative.
2026-07-15: Cold tmux/GDB validation found `rpi5_mmu_parse_fdt()` lost its caller LR when invoking the `reg` decoder: the checked-in DTB produced range `[0, 1 GiB)` and Normal `L2[0]`, then returned into its own `END_NODE` path and stopped with error 3. Preserved/restored the parser LR around helper calls.
2026-07-15: The next tmux/GDB run reached MMU activation with error zero but fetched zero-filled stale instructions at the EL1 vector after enabling I-cache. Added `ic iallu` plus barriers before setting SCTLR_EL1.I so SWD-loaded images do not depend on firmware/debugger cache maintenance.
2026-07-15: After the cache fix was built, the target could not be cold-retested without another power cycle: the prior failed MMU run left the debug core at `vector_table+0x200`, OpenOCD read the loaded text/vector addresses as zero, and a subsequent GDB reload failed. No Unikraft allocator was reached in this run; observed pre-MMU state was DT RAM 640 MiB, rounded Normal bootstrap range 1 GiB, `L2[0]=0x701`, and MMU parser error zero after the LR fix.
2026-07-15: A second cold tmux/GDB run isolated the post-MMU instruction abort to an incorrect `TCR_EL1.T0SZ`: the code used 40 (24-bit VA) while the two-level root/L2 layout requires 24 for a 40-bit VA. Corrected the TCR value from `0x280803528` to `0x280803518`; another cold run is required because the bad translation regime cannot execute the recovery stub.
2026-07-15: Cold tmux/GDB validation after the T0SZ fix succeeded through `uk_boot_entry()` to normal `uk_pm_syshalt`/`lcpu_halt(error_code=0)`. Live state: MMU error 0; DT RAM rounded to `[0,1 GiB)`; root[0]=`0xc1003`; Normal L2[0]=`0x701`; Device L2[64]=`0x0060001000000405`, L2[127]=`0x0060001fc0000405`. Common bootinfo had 8 descriptors, including reserved page zero, 604 KiB kernel, 78,679-byte DTB, and three free regions totaling 639.328 MiB. Buddy allocator reported 639.070 MiB available, hence 264 KiB consumed including allocator/runtime allocations.
2026-07-15: Simplified MMU entry to enforce the firmware contract used by the bootstrap: `SCTLR_EL1.M` and `.C` must both be clear. Removed the partial warm-GDB cache eviction/disable path; an unsupported warm entry now parks with `x0=11` instead of modifying an unknown live cache hierarchy.
2026-07-15: Tmux/GDB testing from the firmware halt state confirmed the strict MMU/cache-off entry check passes. Fixed per-property FDT string bounds after the test exposed error 4 for a valid short property near the strings-block end. The parser now reaches MMU activation; the subsequent synchronous exception at activation still requires investigation.
2026-07-15: Documented `ukplat/pagetable64.S` with the bootstrap translation layout, pre-MMU access constraints, FDT parser register/state contract, descriptor policy, transition coverage checks, MAIR/TCR/SCTLR choices, metadata layout, and GDB-visible failure-code ABI.
2026-07-15: Extracted the bounded pre-MMU FDT header/token walker into `rpi5_mmu_parse_fdt()`. `rpi5_start_mmu()` now calls it after clearing the bootstrap arena, then separately installs the fixed Device aperture, validates transition coverage, and activates translation.
2026-07-15: Made the MMU starter and FDT parser structurally independent: `rpi5_mmu_parse_fdt()` is emitted in its own executable subsection and returns normally, so `rpi5_start_mmu()` continues directly after `bl` without a branch around inline parser code.
2026-07-15: Reordered `ukplat/pagetable64.S` for source-level separation: the complete `rpi5_mmu_parse_fdt()` definition now appears before the complete `rpi5_start_mmu()` definition. The MMU function calls it with `bl` and resumes table/device setup at the following instruction, making the parser straightforward to replace later.
2026-07-15: Expanded the page-table construction and transition-validation comments with the root/L2 hierarchy, 1 GiB index arithmetic, exact 64–128 GiB Device aperture, exclusive-endpoint handling, and the reason each kernel/stack/table/FDT address is checked before enabling translation.
2026-07-15: Documented why `_ukplat_mem_mappings_init()` is intentionally empty: the allocator-time hook has no deferred work because early assembly already builds the complete bootstrap RAM and Device identity map.
2026-07-15: Documented why page zero is reserved: the pointer-returning common allocator cannot distinguish a successful allocation at physical address zero from `NULL`, and common coalescing trims only that page from the overlapping FDT free descriptor.
2026-07-15: Captured the live 4 GiB firmware DTB (2/2 cells; RAM `[0,0x3fc00000)` plus `[0x40000000,0x100000000)`). Coalesced bootstrap block metadata, added split/multinode fixtures, and locally wrapped common FDT bootinfo to decode every enabled memory node/tuple while preserving the 4 MiB hole. The parent Unikraft tree is unchanged.
2026-07-15: Added a separately selectable `APP_RPI5_MEMTEST` beside Hello World and made it the defconfig app. It allocates/fills/verifies 4 MiB chunks toward 4 GiB, reports corruption and the expected capacity shortfall, then frees memory and exits.
2026-07-15: Hardware boot with the live split-bank DTB produced 9 bootinfo descriptors and 4090.953 MiB allocator capacity. Adjusted memtest chunks from 4 MiB to 4 MiB minus 64 bytes after malloc metadata doubled the original buddy allocation; the final run allocated and verified 4075 MiB in 1019 chunks without corruption.
2026-07-15: Addressed MR review: reject RAM tuples that partially overlap the kernel image, and make memtest return success after verifying all allocatable RAM without corruption even when reservations prevent reaching 4 GiB.
2026-07-15: Fixed memtest result precedence so detected corruption fails before the expected capacity-shortfall success path.
2026-07-15: Require memtest to verify at least 4075 MiB so allocation failure or loss of the second firmware RAM bank cannot pass as an expected reservation shortfall.
2026-07-15: Added and tmux-validated the fixed `0x200000` GDB reload trampoline. Hardware testing fixed CLIDR `level * 3`, 32-bit set/way positions, GDB's `BP_HARDWARE_BREAKPOINT` name, cold EL2 detection, unconditional spin release, and BCM2712 debug-AP incoherence by invalidating `[0x80000,0x300000)` plus the DTB after the required set/way clean and SCTLR M/C disable. After one cold install, two consecutive `rpi5_reload` cycles without power cycling each reached the cache-off park, loaded ELF/DTB, printed `main returned 0`, and stopped in `lcpu_halt(error_code=0)`; command: `make && gdb -q -x debug_cfg/ConnectJTAG.gdb`, then `continue` twice per cycle.
2026-07-15: Review hardening fixed the reload park ABI at `0x200400`, made GDB use resident entry/park constants rather than incoming ELF symbols, added a linker assertion for the `[0x80000,0x300000)` image reservation, and rejected DTBs over the reserved `0x14000` bytes before target teardown.
2026-07-15: SemesterProject tmux validation caught and fixed the separated park section's missing branch and BCM2712 OpenOCD ignoring numeric `$pc` writes (the linker-fixed entry symbol works). Two consecutive corrected warm reloads hit `rpi5_reload_park` at `0x200400`, loaded 270,026 ELF bytes plus the 78,679-byte DTB, printed `main returned 0`, and reached the error-zero halt loop without a power cycle.
2026-07-15: Resolved the multi-bank RAM MR against the merged GDB hot-reload changes by retaining both journal histories; `make -j4` verifies the combined tree.
2026-07-15: Updated the debug DTB memory node to the captured 4 GiB firmware layout (`[0,0x3fc00000)` and `[0x40000000,0x100000000)`) so GDB hot reload exercises both RAM banks; rebuilt the DTB and validated memtest plus two warm reloads from merged `main` in the SemesterProject tmux session.
2026-07-16: Added the DT-selected GICv2 and ARM non-secure physical generic timer path (`CNTPCT/CNTP_CVAL/CNTP_CTL`, timer tuple 1), shared common time implementation, and timer stress app. `make -j4` passed; a `SemesterProject` GDB/UART reload probed GIC-400 and completed 1000 sequential 10 ms scheduler sleeps without early wakes or storms (`rpi5-timer-test: PASS`).
2026-07-16: Stored the physical generic timer adaptations as platform-owned patches under `patches/`. `Makefile.uk` applies them to Unikraft common timer sources in `build/unikraft/rpi5-timer/` and links the generated objects without modifying the parent tree. A clean `make` produced `kernel_2712.img`; `compile_commands.json` references only the generated patched timer sources.
2026-07-16: Added Lorin Urbantat to the author and 2026 copyright lines emitted by both patched ARM timer sources.
2026-07-16: Replaced the build-time timer patches with attributed platform-owned `ukplat/time.c` and `ukplat/generic_timer.c`. Removed `ARM_GENERIC_TIMER_PHYSICAL`; RPi 5 now unconditionally uses CNTP registers and DT interrupt tuple 1.
2026-07-16: Documented at the top of both platform timer sources that they are adapted from the corresponding Unikraft `plat/common/arm` implementations and describe the RPi 5 physical-timer specialization.
2026-08-03: Added concise README documentation for Pi 5 GICv2 and physical ARM timer support, including DT interrupt selection, scheduler wake-ups, and hardware validation.
2026-08-13: Mirrored the fs26 AST report scaffold in `../report`: Pandoc defaults, Docker compile script, numbered chapter placeholders, README, and output directory, adapted for the Raspberry Pi 5 Unikraft project.
2026-08-13: Configured `../report/.vscode` to compile the Pandoc report after Markdown or YAML saves with the Emeraldwalk Run on Save extension, and recommended that extension to VS Code.
2026-08-13: Updated `../report/compile.sh` to build the main report into a temporary PDF and append `originality.pdf` to the final `output/report.pdf` with qpdf.
2026-08-13: Filled `../report/chapters/originality.pdf` by crossing the second declaration option and setting the work title to "Porting Unikraft to Raspberry Pi 5"; rebuilt and visually verified the report's final page.
2026-08-13: Added "Urbantat, Lorin" to the first name field in `../report/chapters/originality.pdf`, then rebuilt and visually verified the final report.
2026-08-13: Corrected the originality declaration fields to show surname "Urbantat" under `Name(n)` and given names "Richard Lorin" under `Vorname(n)`; rebuilt and visually verified the report.
2026-08-13: Merged the unrelated platform `main` history into current Unikraft `staging` on `codex/integrate-rpi5`, moved the implementation to in-tree `plat/rpi5`, removed the embedded/generated app, added the catalog-based `Makefile.rpi5` workflow, and updated debug paths/docs. A clean `make -f Makefile.rpi5 verify UK_APP=/tmp/rpi5-catalog.0PCwgA/catalog/native/helloworld-c -j4` produced the Pi 5 image, debug ELF, symbol map, and compile database; platform build files passed checkpatch. Hardware was not rerun because the SemesterProject tmux panes had no active GDB/UART connections.
2026-08-13: Moved the Raspberry Pi 5 implementation to `plat/native/rpi5`, sourced its Kconfig after the native PAL definitions, and updated the in-tree import and wrapper validation paths. A clean catalog Hello World `make -f Makefile.rpi5 verify` regenerated all expected artifacts.
2026-08-13: Replaced the root `README.md` with the exact README from the original platform `main` branch.
2026-08-15: Placed `_librpi5plat_entry` first via `.text.boot`; with the debug feature disabled, an assembly-header lookup table had occupied firmware entry address `0x80000` while code began at `0x80008`.
2026-08-15: Tightened the report evaluation chapter by removing repeated scope statements and the redundant requirement summary while preserving the measured boot, memory, timer, and warm-reload results and their limits.
2026-08-15: Tightened the report discussion chapter by removing repeated evaluation results and compressing platform trade-offs, scope limits, and future work.
2026-08-15: Repeated hardware evaluation on the integrated tree with hot reload enabled: Hello World shut down with error zero; the memory test verified 1,020 chunks (4,278,124,800 bytes) with zero errors; 1,000 sequential 10 ms sleeps passed; and two consecutive warm reloads each reran the timer test and shut down normally. Updated the report with these results.
2026-08-15: Completed the report appendix with the pinned source revisions, hardware setup, application selection, build/load commands, expected pass conditions, and warm-reload procedure needed to reproduce the evaluation.
2026-08-15: Added the tmux-based Codex, GDB, and UART development workflow to the report appendix and documented how safe hot reload enabled rapid physical-hardware iteration.
2026-08-15: Moved the Pi boot `config.txt` into `plat/native/rpi5/` and updated the flash script, README, and report appendix to use the platform-local path.
2026-08-15: Documented why Raspberry Pi 5 bootstrap page tables are derived from the live firmware DTB instead of using KVM/Arm-style compiled machine-layout mappings.
2026-08-15: Expanded the report's peripheral limitations with the implemented PL011/GIC/timer boundary and the missing GPIO, RP1/Ethernet, PCIe, SDIO/Wi-Fi, DMA, interrupt, and Unikraft device-interface support.
2026-08-15: Added the official Unikraft Catalog repository to the report references and cited it from the appendix's `UK_APP` instructions.
2026-08-17: Added platform-local RP1 GPIO scaffolding: runtime-DT discovery and nested `ranges` translation, device-memory mapping when paging is enabled, locked ordered MMIO helpers, GPIO 0--27 direction/read/set/pull API, and Kconfig/README integration. Fixed `Makefile.rpi5 verify` to accept multiple stale symbol maps. Catalog Hello World builds and artifact verification pass; hardware reload reached the platform but the captured DTB probe returned `-EINVAL`, so GPIO electrical validation remains pending.
2026-08-17: Corrected RP1 GPIO handoff and electrical validation. The loaded DT translated RP1 to an inactive CPU window (`0x1f...`, reads `0xdeaddead`), while the preserved BCM2712 outbound window and RP1 BAR mapped it at `0x1c...`. Initialization now validates link state and endpoint `1de4:0001`, restores missing bus numbering, derives GPIO MMIO from the live BAR/window, paces RP1 writes through RC Slot Control, clears GPIO overrides, and uses the bank-0 pad offset. In a fresh timestamped tmux run, Pi GPIO23 HIGH/LOW at 2.135/12.142 s matched ESP32 GPIO15 HIGH/LOW at 14:06:35.101/14:06:45.105.
2026-08-17: Added checked-in `testapps/eval-timer` and `testapps/eval-mem` applications. Verified both with `make -f Makefile.rpi5 verify UK_APP=... -j4`; timer builds the 1,000 x 10 ms scheduler evaluation and mem builds the 4 GiB allocation/fill/verify test.
2026-08-17: Added checked-in `testapps/hello-world` smoke-test application and verified it with `make -f Makefile.rpi5 verify UK_APP=testapps/hello-world -j4`.
2026-08-17: Removed the unused `LIBUKBUS_PLATFORM` dependency and `uk_bus_pf_devmap`/paging conditionals from RP1 GPIO; the platform uses direct MMIO and does not plan to enable paging. `make -f Makefile.rpi5 verify UK_APP=testapps/rpi5-gpio -j4` passed.
2026-08-17: Simplified the GPIO API to `rpi5_gpio_direction(gpio, enum rpi5_gpio_direction)`; output level is now set separately with `rpi5_gpio_set()`. Updated the GPIO test and verified the Pi 5 image, symbols, and compile database.
2026-08-18: Simplified RP1 GPIO initialization to the fixed firmware BAR contract shared with PL011 (`0x1c00000000`): GPIO/RIO/pads use offsets `0xd0000/0xe0000/0xf0000`. Removed runtime DT parsing, PCIe BAR/bus discovery, root-complex repair, and PCIe write pacing. Updated the handoff notes and verified the Pi 5 image, symbols, and compile database.
2026-08-18: Added and uploaded an interactive HTML checklist covering the 31 subsection review recommendations; completion state is persisted in browser local storage.
2026-08-18: Added an expandable alternative formulation to every checklist point and uploaded the revised HTML checklist.
2026-08-25: Removed the development-only pre-MMU checks that re-read the bootstrap page table to confirm Normal mappings for the kernel, stack, translation tables, and DTB. Removed the unused helper and error code 10 path.
2026-08-26: Ported dynamic paging to `staging`: enabled native ARM64 paging, changed the bootstrap to 48-bit geometry, added the paging direct map, and described the RP1 DMA and BCM2712 MMIO ranges as Device-nGnRnE. Moved GPIO initialization after the managed-table switch and added `rpi5_config/rpi5_nopaging_defconfig` as the fixed-map fallback.
2026-08-26: Moved RPi 5 early boot initialization before dynamic paging so bootinfo memory descriptors are coalesced before FREE regions are marked unmapped.
2026-08-26: Remapped RPi 5 device regions locally after the managed page-table switch so Device-nGnRnE replaces the initial Normal-WB mappings without modifying another platform.
2026-08-26: Removed the incorrect Device-nGnRnE mapping and memory descriptor for `[3 GiB, 4 GiB)`; DT-reported RAM there now remains Normal WBWA. The 64–128 GiB BCM2712 peripheral aperture remains Device-nGnRnE.
2026-08-26: Added `testapps/dynamic-paging` and a dedicated defconfig. The hardware test maps an allocated page at 2 TiB, checks read-only and read/write permissions, verifies address translation, unmaps it, and confirms that no-fault access fails.
2026-08-26: Moved the dynamic paging test defconfig beside the test application at `testapps/dynamic-paging/defconfig` and updated the documented build command.
