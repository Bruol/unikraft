2026-05-18: Added external Unikraft platform wiring in this repo (`Config.uk`, `Makefile.uk`, `Linker.uk`, `ukplat/*`) plus a tiny default app and `configs/rpi5_defconfig`. Updated top-level `make` to invoke `gmake -C ../unikraft A=$PWD/app P=$PWD O=$PWD/build/unikraft C=$PWD/app/.config UK_CFLAGS=-std=gnu11`. `make` now completes and emits `build/unikraft/kernel_2712.img`; remaining warnings are Unikraft Kconfig warnings about unrelated choice/imply symbols and native TLB selection without paging.
2026-05-18: Verified generated files with `ls -lh build/unikraft/app_rpi5-arm64 build/unikraft/app_rpi5-arm64.dbg build/unikraft/kernel_2712.img`; output image is about 243 KiB and debug ELF is about 1.2 MiB.
2026-05-18: Fixed fresh-tree `make` failure where Unikraft normalized missing `build/` to `/unikraft`; added `unikraft-build-dir` target that runs `mkdir -p $(UK_BUILD_DIR)` before invoking `gmake`. Verified by removing `build/unikraft app/.config` and running `make`, which rebuilt `build/unikraft/kernel_2712.img`.
2026-05-18: Removed assumed BSD-3-Clause SPDX headers from newly added `ukplat/*` and `app/main.c`. Verified no SPDX headers remain in `Config.uk Makefile.uk Linker.uk ukplat app configs src` with `rg`, then reran `make` successfully.
2026-05-19: Added Unikraft platform early spin loop in `ukplat/entry64.S` using `x2` as the continue flag, ported the bare-metal PL011 UART into `ukplat/pl011.[ch]`, added UART boot breadcrumbs around relocation/pcpu/FDT/setup, and updated `debug_cfg/ConnectJTAG.gdb` with `rpi5_reload` to reload the Unikraft debug ELF and restore `x0` from `debug_cfg/bcm2712-rpi-5-b.dtb` at `0x10000000`. Verified with `make`; it rebuilt `build/unikraft/kernel_2712.img`.
2026-05-19: Reran `make` after changing `debug_cfg/ConnectJTAG.gdb` to use `build/unikraft/app_rpi5-arm64.dbg`; build completed with no new compile work, only the existing Kconfig warnings.
2026-05-19: Added `CONFIG_RPI5_EARLY_GDB_SPINLOOP`, default `y`, to guard the early `x2` GDB spin loop in `ukplat/entry64.S`. Verified with `make` and confirmed `CONFIG_RPI5_EARLY_GDB_SPINLOOP=y` in `app/.config` and `build/unikraft/config`.
2026-05-19: Inspected configs/rpi5_defconfig and its Makefile/Kconfig references to explain the default RPi 5 Unikraft build configuration.
2026-05-19: Updated `debug_cfg/ConnectJTAG.gdb` and its `rpi5_reload` helper to set `$x2 = 0` after restoring `$pc` and `$x0`, so the early GDB spin loop is released by the script. No rebuild needed.
2026-05-19: Moved `file build/unikraft/app_rpi5-arm64.dbg` before `target extended-remote :3333` in `debug_cfg/ConnectJTAG.gdb` to avoid GDB warning that no executable was specified before connecting. No rebuild needed.
2026-05-19: Added `rpi5_pl011_dump_mem()` and call it from `ukplat/entry64.S` after printing the DTB pointer to dump the first 64 bytes at `x0`. Verified with `make`; it rebuilt `build/unikraft/kernel_2712.img` with only the existing Kconfig warnings.
2026-05-19: Added EL2-to-EL1 drop, `VBAR_EL1=vector_table`, KVM ARM exception vectors, GICv2 config, PL011 console registration, BSP lcpu init, and removal of the low `vbase=0` free memory region before boot. Verified via `tmux` GDB/tio run: after clearing `rpi5_gdb_spin_flag`, UART reaches `uk_boot_entry()`, and interrupting shows `uk_boot_entry -> uk_pm_shutdown -> uk_pm_syshalt -> lcpu_halt(error_code=0)`.
2026-05-19: Changed the early GDB spin loop to re-read `rpi5_gdb_spin_flag` each pass and updated `debug_cfg/ConnectJTAG.gdb` to release it with `set {unsigned long long}&rpi5_gdb_spin_flag = 0`. Verified with `make`; in `tmux` GDB, manually setting that memory from `1` to `0` released the loop and hit `rpi5_pl011_init`.
2026-05-19: Clarified the `CONFIG_RPI5_EARLY_GDB_SPINLOOP` help text to mention clearing either `x2` at the loop `nop` or `rpi5_gdb_spin_flag`; no rebuild needed.
2026-05-19: `main()` was reached under GDB but `printf()` did not appear on UART because `ukplat/pl011.c` used `CONFIG_LIBUKCONSOLE` without including `uk/config.h`, compiling the console registration path out. Added the include and rebuilt with `make`. Live rerun was blocked after OpenOCD entered a stale FP/SIMD register state (`Failed to write v0 register`) and restarting OpenOCD required sudo password input in tmux pane `SemesterProject:0.0`.
2026-05-28: Restored overwritten `ukplat/entry64.S` from the prior RPi5 bring-up version: EL2-to-EL1 drop, `VBAR_EL1=vector_table`, optional `rpi5_gdb_spin_flag` loop, boot stack/BSS setup, PL011 breadcrumbs, DTB dump, pcpu/FDT setup, and branch to `rpi5_ukplat_entry`. Verified with `make`; it rebuilt `build/unikraft/kernel_2712.img` with only existing Kconfig warnings.
2026-05-28: Added BSD-3-Clause headers to Unikraft-adapted `ukplat/entry64.S`, `ukplat/link64.lds.S`, and `ukplat/memory.c`, with original `ukplat/entry64.S`/`ukplat/link64.lds.S` attribution to Wei Chen and 2026 local-modification copyright for Lorin Urbantat. Verified with `make`, which rebuilt `build/unikraft/kernel_2712.img`.
2026-05-28: Added Lorin Urbantat to the `Authors:` blocks in `ukplat/entry64.S` and `ukplat/link64.lds.S`. Verified with `make`, which rebuilt `build/unikraft/kernel_2712.img`.
2026-05-28: Removed the legacy baremetal boot path (`src/*` and `linker.ld`) and stripped the top-level Makefile baremetal/disasm/flash-baremetal build rules. Verified with `make`, which completed the remaining Unikraft build with only the existing Kconfig warnings.
2026-07-15: Removed the no-op early `do_uk_reloc` path from the fixed-address RPi5 entry, added a pre-VBAR runtime check that parks with the actual base in `x1` and required base in `x2` when the image is not loaded at its linked address, and made `Makefile.uk` reject `CONFIG_OPTIMIZE_PIE=y` for `PLAT_RPI5`.
2026-07-15: Fixed the app build by including the private `app/bruol.h` header with a quoted include. Verified with `make`.
2026-07-15: Added DTB-driven EL1 MMU bootstrap in `ukplat/pagetable64.S`: validated the FDT, decoded enabled root memory `reg` tuples, built 1 GiB Normal identity blocks plus the fixed 64–128 GiB Device window, exported rounded ranges, and enabled TTBR0/MAIR/TCR/SCTLR. Entry now gates Pi 5 cores by MPIDR Aff1 and calls MMU setup before BSS clearing; memory unmap metadata uses the single parsed range. `make -j4` emits `kernel_2712.img`. A tmux/GDB hardware run found and fixed pre-MMU unaligned string loads (ESR `0x96000021` at DT strings `0x100116c4`) by switching comparisons to byte loads; final cold-entry hardware verification remains pending because OpenOCD cannot reset this target and the debug core is currently inside the prior exception context.
2026-07-15: Removed the C-side DTB-range-to-`bpt_unmap_mrd` translation and the deletion of the entire low free region. The platform now supplies only the fixed pre-device bootstrap aperture, reserves page zero through the common memregion insert/coalesce logic, and calls `ukplat_mem_init()` before boot handoff so common memory trimming/allocation stays authoritative.
2026-07-15: Simplified MMU entry to enforce the firmware contract used by the bootstrap: `SCTLR_EL1.M` and `.C` must both be clear. Removed the partial warm-GDB cache eviction/disable path; an unsupported warm entry now parks with `x0=11` instead of modifying an unknown live cache hierarchy.
2026-07-15: Tmux/GDB testing from the firmware halt state confirmed the strict MMU/cache-off entry check passes. Fixed per-property FDT string bounds after the test exposed error 4 for a valid short property near the strings-block end. The parser now reaches MMU activation; the subsequent synchronous exception at activation still requires investigation.
2026-07-15: Documented `ukplat/pagetable64.S` with the bootstrap translation layout, pre-MMU access constraints, FDT parser register/state contract, descriptor policy, transition coverage checks, MAIR/TCR/SCTLR choices, metadata layout, and GDB-visible failure-code ABI.
2026-07-15: Extracted the bounded pre-MMU FDT header/token walker into `rpi5_mmu_parse_fdt()`. `rpi5_start_mmu()` now calls it after clearing the bootstrap arena, then separately installs the fixed Device aperture, validates transition coverage, and activates translation.
2026-07-15: Made the MMU starter and FDT parser structurally independent: `rpi5_mmu_parse_fdt()` is emitted in its own executable subsection and returns normally, so `rpi5_start_mmu()` continues directly after `bl` without a branch around inline parser code.
2026-07-15: Reordered `ukplat/pagetable64.S` for source-level separation: the complete `rpi5_mmu_parse_fdt()` definition now appears before the complete `rpi5_start_mmu()` definition. The MMU function calls it with `bl` and resumes table/device setup at the following instruction, making the parser straightforward to replace later.
2026-07-15: Expanded the page-table construction and transition-validation comments with the root/L2 hierarchy, 1 GiB index arithmetic, exact 64–128 GiB Device aperture, exclusive-endpoint handling, and the reason each kernel/stack/table/FDT address is checked before enabling translation.
2026-07-15: Documented why `_ukplat_mem_mappings_init()` is intentionally empty: the allocator-time hook has no deferred work because early assembly already builds the complete bootstrap RAM and Device identity map.
2026-07-15: Documented why page zero is reserved: the pointer-returning common allocator cannot distinguish a successful allocation at physical address zero from `NULL`, and common coalescing trims only that page from the overlapping FDT free descriptor.
